package com.superpets.mobile.core.utils

/**
 * Platform-specific utilities for handling app permissions
 */
expect object PermissionUtils {
    /**
     * Opens the app's settings page where users can manage permissions
     */
    fun openAppSettings()
}
